{
  "name": "@xenonbyte/forma-agent",
  "version": "0.1.7",
  "private": true,
  "description": "Workspace-only agent command templates bundled into @xenonbyte/forma-cli during packaging.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@xenonbyte/forma-core": "workspace:*"
  }
}
