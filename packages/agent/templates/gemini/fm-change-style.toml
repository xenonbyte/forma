description = "Change the brand/system style of a Forma product's design system by persisting config then fully regenerating the component library via MCP, then self-review."
prompt = """
# Forma route: fm-change-style

Use shared Forma guidance at ~/.forma/skills/forma/SKILL.md.

Change the `brand_style` / `system_style` for an entire product. This command: (1) persists the new style config with `update_product_config`, then (2) fully regenerates the component library (design system) using the same generation flow as fm-refine-components — inlined here. Design-page artifacts are NOT touched.

Preconditions (tier 1):
- Tier 1: a product must exist and be selected.
If the core tool returns `REQUIREMENT_NOT_FOUND` or `REQUIREMENT_STATUS_INVALID` in an unexpected context, report the error faithfully.

Execution:
1. Require product_id from context or ask the user to run `fm-list-product` first.
2. Confirm the new `brand_style` and optional `system_style`. If not already chosen, call `list_styles` and confirm with the user.
3. Persist the style config: first call `get_product(product_id)` to read the existing `platform`, `languages`, and `default_language`. These product config fields are optional on freshly created products. Before calling `update_product_config`, require concrete values: `platform` is set, `languages` is a non-empty list, and `default_language` is set and included in `languages`. If any missing product config field is absent or empty, ask the user to provide/confirm the missing `platform`, `languages`, and/or `default_language`; do not infer silent defaults. Do NOT pass undefined, null, or empty values to `update_product_config`. Then call `update_product_config(product_id, platform, brand_style, system_style, languages, default_language)` carrying the existing or user-confirmed values forward with the new style. This is the authoritative config write. If this step fails, stop and report the error — do not continue.

   **Partial-failure boundary**: if `update_product_config` succeeds but any subsequent step (export, generation, save, or self-review) fails, STOP immediately and report:
   > "产品配置已更新但当前组件库可能仍是旧版本/未刷新（partial update: product config saved but component library may be stale）。请重跑 fm-refine-components 或 fm-change-style 以刷新组件库指针版本。"
   Do NOT continue to trigger fm-design or claim the style change is fully applied.

4. Load the current component library for re-skin when one exists (inlined fm-refine-components flow):
   - Call `list_product_artifacts(product_id, kind="component-library")` to find the current non-superseded library. Do not use legacy/superseded artifacts as a re-skin source; `generate_components` writes to the current product pointer or creates the first current library.
   - If no current component library is returned, continue with initial generation: no `source_html`, no existing `manifest.forma.productIcon.shape`, and no export step. This is a normal first-time refresh after the config write, not a partial failure.
   - If a current library exists, call `get_product_artifact(product_id, artifact_id)` to read its manifest (recover `forma.productIcon.shape` for ICON geometry reuse when present).
   - If a current library exists but `manifest.forma.productIcon` or `manifest.forma.productIcon.shape` is missing, keep using the current library as the re-skin source and still perform the export step below: preserve the exported `source_html` for the component/foundations refinement and treat only the Product ICON as initial icon creation. This is not a partial failure.
   - If a current library exists, call `export_artifact(product_id, artifact_id, format="html")` and read the returned `output_path` as `source_html`. If the export response notes omitted assets, also call `export_artifact(product_id, artifact_id, format="zip")` and inspect the bundle. Stop and report the partial-failure message (step 3 boundary) if the source HTML cannot be loaded.
5. Fetch style knowledge BEFORE generating: call `get_style(brand_style)` (returns DESIGN.md, tokens.css, components.html). If `system_style` is set, call `get_style(system_style)` too.
6. Call `get_component_baseline(product_id)` to obtain the authoritative spec: `{ platform, baseline: { foundations, productIcon, components } }`. Use this as the sole source of WHAT to produce.
7. Palette design-read (before defining brand tokens): read `craft_rules` from the `get_component_baseline` response. Apply the palette-rotation rule from ai-tells: do not default to warm beige/cream with brass/clay/oxblood unless brand_style tokens already lock that palette. Rotate real alternatives and record the chosen direction in one line.
8. Regenerate the component library as **shared CSS (`tokens_css`) + an ordered list of units (`units`)**, applying the new style. The new brand/system tokens drive `tokens_css`; the units carry structure. Do NOT produce a single combined HTML document.
   - **`tokens_css`** (a single CSS string): derive ALL shared styling from the new `brand_style` (+ `system_style`) tokens — design tokens/variables, base styles, component classes, fonts, and `@font-face` declarations. Every class or CSS variable referenced by any `body_html` fragment must be defined here. No `<style>` tags go into `body_html`.
   - **`units`** — emit exactly the following units IN THIS ORDER:
     a. `{ id: "foundations", role: "foundations", title: "Foundations", body_html: <fragment> }` — token visualization for every category in `baseline.foundations`, using the NEW tokens.
     b. `{ id: "icon", role: "icon", title: "<Product> Icon", body_html: <fragment> }` — product-icon showcase (primary full-color + monochrome). The actual SVG files still go through `product_icon` + `supporting_files` in step 9 (unchanged). ICON rules: if a current manifest with `manifest.forma.productIcon.shape` was loaded in step 4, recover the existing persisted `shape` (`shapeId` + `geometry` + `sourceVersion`). Reuse that geometry exactly and only recolor per the new brand tokens. Keep `shapeId` stable. When saving with `generate_components`, convert the persisted camelCase fields to the tool's snake_case input fields. Do NOT redraw the mark from scratch during re-skin. If no current library existed, or if `manifest.forma.productIcon` or `manifest.forma.productIcon.shape` is missing, treat only the ICON as initial icon creation: create a new SVG mark, assign a stable `shape_id`, render both variants, and preserve the existing `source_html` component structure when `source_html` exists. Prepare both primary and monochrome variants as separate SVG files for `supporting_files`.
     c. One `{ id: "<component-slug>", role: "component", title: <component name>, body_html: <fragment> }` per entry in `baseline.components`, covering all listed states/variants. The `id` slug MUST match `^[a-z0-9][a-z0-9-]{0,63}$`. When `source_html` exists (exported in step 4), PRESERVE each component's existing structure/markup — carry it into the corresponding unit's `body_html`, applying the new style ONLY through the new `tokens_css` (do not restructure the markup; re-theme via CSS). When no `source_html` exists, generate the baseline component set from scratch.
   **Each `body_html` MUST be a pure markup fragment** — no `<html>`, `<head>`, `<body>`, or `<style>` tags. It must render correctly using ONLY the classes and CSS variables defined in `tokens_css`.
   Pure-static contract: no `<script>`, no inline `on*` handlers, no `javascript:` URLs, no external scripts/stylesheets; inline images as `data:` URLs; no remote URLs anywhere. This applies to each `body_html` fragment and to `tokens_css`.
9. Save the component library with `generate_components`, passing:
   - `product_id`, `title`, `brand_style`, `system_style`
   - `tokens_css`: the shared CSS string containing all shared styles (tokens, component classes, fonts)
   - `units`: the ordered array of unit objects — `[ { id, role, title, body_html }, … ]` — as produced in step 8
   - `product_icon`: `{ primary: "assets/icon.svg", monochrome: "assets/icon-mono.svg", shape: { shape_id: <stable-id>, geometry: <reusable SVG inner markup>, source_version: <string> } }`
   - `supporting_files`: `[ { path: "assets/icon.svg", content_type: "image/svg+xml", content_base64: <base64 of primary SVG> }, { path: "assets/icon-mono.svg", content_type: "image/svg+xml", content_base64: <base64 of monochrome SVG> } ]`
   **Do NOT include a top-level `html` field** — the `tokens_css` + `units` fields replace it entirely.
   If this step fails, stop and report the partial-failure message from step 3.
   The tool returns `artifact_id`, `version`, `preview_status`.
10. Self-review (MANDATORY):
   - Read it back: `get_product_artifact(product_id, artifact_id)` and inspect `manifest.forma.quality.craftChecks`.
   - For any check with `passed:false`, fix that specific violation and call `generate_components` again until the checks pass.
   - Also judge the non-mechanical craft items the lint cannot (type hierarchy, color restraint, contrast, state coverage, form validation, motion, UX laws); regenerate if any fails.
   - If self-review fails after repeated attempts, stop and report the partial-failure message from step 3.
11. Report `artifact_id`, the final `version`, `preview_status`, and a short craftChecks summary. End here — do NOT trigger fm-design or any follow-up commands.
"""
