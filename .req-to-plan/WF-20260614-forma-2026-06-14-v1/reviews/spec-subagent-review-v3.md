# SPEC Subagent Review — v3 (verification of applied fixes)
Reviewer: read-only subagent · Date: 2026-06-14

## Verdict
APPROVE — all five required changes (2 blocking + 2 advisory + 1 itemization) are correctly applied in v3, live-code anchors confirm them, and the v2→v3 edits introduced no new breakage, duplicate ID, or DECISION-001/002 contradiction.

## Fix Verification
- **BLOCKING-1 (schema placement):** Applied correctly. SPEC-DATA-002 (06-spec.md:77-82) now reads: `// 注意：productSchema 形如 base.extend({...}).strict().superRefine(...)（结果是 ZodEffects，无 .extend()）` and `// brand_assets 字段必须加进现有 .extend({...}) 的对象里（在 .strict() 之前），不可追加到链尾`. It also states `brand_assets 为 optional，不参与 superRefine` and gives both targets explicitly (`productSchema 内: brand_assets: brandAssetsSettingsSchema.optional()` and `productConfigSchema ... 同样加进其对象定义内`). The misleading "append to the .strict() chain" wording is gone; ZodEffects/no-chainable-`.extend()` caveat and the superRefine-untouched note are both present. ✅
- **BLOCKING-2 (dangling test import):** Applied + live-code confirmed. SPEC-DATA-008 test bullet (:146) now reads `并修 packages/core/tests/brand-assets.test.ts：移除 STORE_SHOT_PRESETS import(:31) 与用例(:630)（否则 typecheck 断）`. Live file confirms: the import `STORE_SHOT_PRESETS,` sits in the import block ending at line 31, and the usage `const preset = STORE_SHOT_PRESETS["web-og"];` is at line 630 (inside the "renders a store-shot at the EXACT pixels of a known preset" test). Both exactly as the SPEC now lists. ✅
- **Advisory return-union:** Applied. SPEC-DATA-006 (:128-133) now adds the explicit `SaveBrandAssetResult` discriminated union: `{ kind:"app-icon"; assets: BrandAssetRecord[] }` (整套派生记录) vs `{ kind:"store-shot"|"banner"|"poster"; asset: SavedBrandAsset }` (单条), and the closing note states "输入与返回均按 kind 判别". TM-07 (:169) asserts the app-icon `{assets}` return + no-accumulate-on-replace. ✅
- **Advisory resolveBrandImageRef rule + TM-07:** Applied + live-code confirmed. SPEC-DATA-008 (:150) now adds the "额外（resolveBrandImageRef bare-ref 选择规则，替换 MASTER_SIZE 依赖）" block: it names the obsolete `MASTER_SIZE=2048` dependency at `:747`, explains it disappears with the 1080 work-canvas pipeline, and defines the deterministic rule "选该 product app-icon 记录中 variant 为 standard 类、尺寸最大的那个文件" (web→standard largest, mobile/tablet→that surface's standard largest), with "无匹配则 FormaError". TM-07 (:169) asserts `resolveBrandImageRef("forma-image://brand/app-icon")` (no @size) selects by "standard 最大档" 不依赖 MASTER_SIZE, 无匹配报 FormaError. Live code confirms `MASTER_SIZE` IS referenced inside `resolveBrandImageRef` at line 747 (`const wantWidth = size ?? MASTER_SIZE;`; `MASTER_SIZE = 2048` defined at :129, used for the bare-ref default), exactly the dependency the new rule replaces. ✅
- **store.ts itemization:** Applied. SPEC-DATA-008 (:144) now itemizes three distinct sites instead of "~4 行": `listStoreShotPresets 转发方法（接口方法 :110 + 实现）、StoreShotPreset 的 value-import(:11) 与 type-import(:18) ——逐处清理，非笼统"~4 行"`. Covers value-import + type-import + interface method + implementation as the v2 review required. ✅

## Regression Check
- none. No SPEC ID is duplicated (each SPEC-BEHAVIOR-001..009, SPEC-DATA-001..008, TM-01..13 appears exactly once). Internal cross-references stay coherent: SPEC-DATA-006↔SPEC-BEHAVIOR-005 (discriminated union), SPEC-DATA-008's new bare-ref rule↔TM-07 (both cite "standard 最大档" / not-MASTER_SIZE), SPEC-DATA-002↔DECISION-001 (posters all default `true`), SPEC-BEHAVIOR-001↔DECISION-002 (gemini default gated on verified compatibility, openai→gpt-image-1.5 unconditional). No contradiction with DECISION-001 (666² safe-area, desktop {1024..16}, poster 1080×1920/1920×1080/1080×1080, posters all-true) or DECISION-002 (verify-then-switch). The v3 edits are additive/clarifying within SPEC-DATA-002/006/008 and TM-07; no surrounding contract was disturbed.
