# Plan Subagent Review (v2)

## Verdict
APPROVE_WITH_NITS — All 21 SPEC IDs consumed by implementing tasks, all 16 SCOPE-IN closed, every modify/delete target exists and every create target is genuinely new; the only real gap is that several "三平台同改" tasks list only the claude template in `Files:` (major-consistency, not blocking).

## Coverage
- **SPEC IDs (21/21)**: `grep "^Spec References:"` yields exactly SPEC-BEHAVIOR-001..015 + SPEC-DATA-001..006, each appearing in a task whose Steps actually realize it (see Task↔spec fidelity). No orphan, no double-count mismatch vs the Trace table (07-plan.md:409-425).
- **SCOPE-IN (16/16)**: every SCOPE-IN-001..016 appears in a "关闭 SCOPE-IN-xxx" step. Multi-task splits are coherent: SCOPE-IN-007 spans T003 (manifest)/T005 (pointer write)/T007 (generation template); SCOPE-IN-002 spans T011 (补段)/T012 (parity guard); SCOPE-IN-016 spans T016 (page/mapper)/T017 (route/entry).
- **No contract is consumed-but-not-implemented.** Spot-checked the load-bearing attachments — each task's Steps/Skeleton match the SPEC contract it cites.
- SCOPE-OUT references all live in the read-only Upstream Summary block (07-plan.md:556+), never in a PLAN-TASK action item. No scope creep.

## File-path correctness
Verified all listed files under repo_root via `ls`/`grep`:
- **create-tasks target genuinely-new paths** (all MISSING, correct):
  - PLAN-TASK-004 `packages/core/src/component-baseline.ts` + test — MISSING ✓
  - PLAN-TASK-012 `packages/agent/tests/template-parity.test.ts` — MISSING (no `agent/tests/` dir yet) ✓
  - PLAN-TASK-016 `packages/web/src/pages/BrandResources.tsx`, `packages/web/src/viewer/brandResourcesMapper.ts`, `BrandResources.test.tsx` — all MISSING ✓; sibling `packages/web/src/viewer/` dir exists with `mapArtifacts.ts`, so the create location is valid.
- **modify/delete-tasks target existing files** (all EXIST): PLAN-TASK-001 agent templates (6 deletions) + `agent/src/index.ts` + `core/src/install.ts` + `mcp/src/tools.ts` + `core/src/product.ts` + `shared/SKILL.md` + tests; PLAN-TASK-003 `artifact-manifest.ts`; PLAN-TASK-005 `store.ts`; PLAN-TASK-006 `design-context.ts` + `tools.ts`; PLAN-TASK-013 `VersionCompare.tsx`/`routes.tsx`/`DesignView.tsx`/`i18n.ts`/`DesignView.test.tsx`; PLAN-TASK-014 `AnnotationPage.tsx`; PLAN-TASK-015 `Canvas.tsx`/`Canvas.browser.test.tsx`; PLAN-TASK-017 `ProductDetail.tsx`. **Zero misclassifications.**

## Task↔spec fidelity
The load-bearing tasks genuinely realize their contracts:
- **PLAN-TASK-005 (pointer activation, the load-bearing change)** — CONFIRMED accurate. `store.ts:220 generateComponents` currently calls `saveDesignArtifact` with no `artifactId` and never calls `setDesignSystemArtifactPointerLocked` (verified body 220-237). The task's Steps ("首次 setDesignSystemArtifactPointerLocked；后续读 designSystemArtifactId 作 input.artifactId 追加版本") exactly close the "no writer" gap the DESIGN identified. `product.ts:61` has the field, `:270` the setter — both present.
- **PLAN-TASK-006 (get_component_baseline + get_design_context ext + single-source收口)** — faithful. `design-context.ts:69` returns `{craft, brandStyle, systemStyle, page, rules, platform, language}` (no componentBaseline/componentLibrary), matching the "add two fields" claim. `getBaselinePage` (`tools.ts:1225`) reads `designSystemArtifactId` and throws ARTIFACT_NOT_FOUND when unset — confirming the "currently always throws, success-path test must be added" sub-step.
- **PLAN-TASK-008 (remove change_artifact_style)** — faithful. `store.ts:84` interface, `:240` `changeArtifactStyle`, `:257` `changeArtifactStyleWithManifest`, `:307` export all exist; `tools.ts` has the name/schema/desc/handler (lines 59/343/377/466). Step enumerates all of these.
- **PLAN-TASK-001 (deletions + guard)** — faithful and the grep claim is **verified**: `rollbackDesignPointerLocked` has exactly one real source caller, `tools.ts:1017` (the `rollback_requirement_design` handler being deleted). The only other matches are `packages/core/dist/product.d.ts` (stale build output) and the definition itself — so it becomes true dead code after the handler is removed. Guard skeleton correctly asserts absence of create_product/delete_product/rollback/change_artifact_style.
- **PLAN-TASK-003** — `forma.productIcon` skeleton uses `shape.geometry` carrying SVG markup (not just an id), matching SPEC-DATA-001's "携带几何本体" determination. `ArtifactFormaExtension` at `artifact-manifest.ts:73`, `validateFormaExtension` at `:104` — both as cited.
- **PLAN-TASK-013** — i18n compare keys verified: exactly 6 keys × 2 locales = 12 entries (`i18n.ts` en 89-94, zh 381-386), matching "6 个对比 i18n 键(en+zh 12 条)". DesignView compare link + `version_count>=2` filter present (lines 56-59, 110-118).

## Sequencing
Honored. Top-of-file header (07-plan.md:11) states T001(删除批) → T002–T009(B 批) → T010(R3) → T011/T012(R2 守卫最后锁) → T013–T017(web), matching the SPEC排期 R1/R4/R5 → B1–B7 → R3 → R2 and "BC3 after pointer work" (T016 reuses T005 pointer + T003 manifest + T015 Canvas).
- **DesignView.tsx serialize concern IS flagged in both tasks**: PLAN-TASK-013 step (line 316) "与 PLAN-TASK-015 同改 DesignView.tsx → 串行/合并为一个改动批"; PLAN-TASK-015 step (line 359) "DesignView.tsx 宿主改动与 PLAN-TASK-013 合并为一个改动批避免冲突". Matches RISK-DEP-003.
- R2 parity guard (T012) is correctly placed last so it locks the final 6-command canonical set after all B-batch template edits.

## Findings
1. **[major] Incomplete `Files:` list on "三平台同改" template tasks.** Location: PLAN-TASK-002, -007, -008, -009, -010. Each task's Steps explicitly mandate "三平台同改（claude/codex/gemini）", and the codex/gemini sibling templates all EXIST (`codex/fm-change-style/SKILL.md`, `gemini/fm-change-style.toml`, `codex/fm-refine-components/SKILL.md`, `gemini/fm-refine-components.toml`, `gemini/fm-design.toml`, etc.), but the `Files:` sections list ONLY the `claude/*.md` template (+ `shared/SKILL.md`). Risk: an executor following `Files:` literally edits only claude and drifts codex/gemini. The T012 parity guard catches this — but T012 lands LAST, so intermediate B-batch commits are unprotected (exactly RISK-REG-001). Fix: add the codex `<cmd>/SKILL.md` and gemini `<cmd>.toml` siblings to the `Files:` of T002/T007/T008/T009/T010, OR add a one-line note that "三平台同改" implies the codex/gemini siblings for every claude template listed.
2. **[minor] Cited DesignView line range slightly stale.** Location: SPEC-DATA-006 / DES-ARCH-009 cite `DesignView.tsx:115-118` and `:59`. Live code: the `version_count>=2` filter is at lines 56-59 and the compare link block spans 110-118 (href at 115). Not wrong enough to mislead, and the SPEC explicitly says "按现网复核行号" (verify line numbers against live code at impl time). No action required beyond honoring that instruction.
3. **[nit] PLAN-TASK-016 does not list `ProductDetail.tsx`/`routes.tsx`/`i18n.ts`** — but that's intentional: those belong to PLAN-TASK-017 (entry + route wiring). The split is clean (016 = page+mapper, 017 = entry+route+i18n). No fix needed; noting so the executor doesn't expect routing to work after 016 alone.
4. **[nit] PLAN-TASK-006 Files omits an explicit `core/tests/design-context.test.ts`** for the new `componentBaseline`/`componentLibrary` assertions; the SPEC Test Matrix lists a `core/tests`(design-context) row. The task folds design-context coverage into `mcp/tests/tools.test.ts`. Acceptable (mcp test exercises buildDesignContext end-to-end), but a dedicated core unit test would be cleaner. Optional.

## Notes for implementation
- **Three-platform edits are the #1 watch-out.** For every `claude/<cmd>.md` you touch in T002/T007/T008/T009/T010, also edit `codex/<cmd>/SKILL.md` and `gemini/<cmd>.toml` with the identical de-shelled body — the `Files:` lists under-specify this. Land T012 ASAP after the B batch (or run it locally during B-batch edits) to catch drift before it escapes CI.
- **T005 is the lynchpin.** `generateComponents` currently takes no `artifactId` argument and `saveDesignArtifact` keys off `input.artifactId ?? generateArtifactId()`. You must thread the existing `product.designSystemArtifactId` into `saveDesignArtifact`'s input and add the first-time `setDesignSystemArtifactPointerLocked` call. Once landed, `get_baseline_page`/`get_baseline_image` flip from always-throw to returning data — add the success-path tests T006 calls for (there is currently no test asserting they throw, so nothing breaks, but the behavior flip must be covered).
- **T001 grep before delete (already mandated):** confirmed `rollbackDesignPointerLocked`'s only live caller is the handler at `tools.ts:1017`; safe to delete after the handler. Ignore the `dist/` hit — rebuild clears it.
- **DesignView.tsx (T013 + T015):** execute as a single merged change-batch as both tasks instruct; do not let them run as parallel branches.
- **Protected boundaries respected:** no task touches `store.deleteProduct`, HTTP `DELETE /api/products`, `ConfirmDeleteDialog`, the 4 MCP read tools, the requirement state machine, the immutable `v{n}`/`current_version` version mechanism, or the canvas engine. DECISION-001=B (no migration) is honored — T005/T009 treat an unset pointer as "no current component library" with the legacy two-stage stop, no backfill.
