# P7 — 共享 viewer 包 `@xenonbyte/forma-viewer` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 monorepo 内共享 React 包 `@xenonbyte/forma-viewer`——一个纯展示、数据驱动的无限画布查看器(左=设计稿列表 / 中=画布 / 右=标注 slot),设计画布渲染静态 HTML(`<iframe sandbox>` 禁脚本)、标注画布渲染 PNG(显式 1x/2x LOD),web 与 desktop 经同一规范化 view-model 契约 + 注入式资源解析器同源消费、无分叉。

**Architecture:** 包分两层——**纯逻辑层**(view-model 类型契约、规范化、tile 布局计算,无 DOM,node 环境可测)与**组件层**(React Flow 无限画布外壳、两种 tile 渲染器、左列表定位跳转、右标注 slot 占位,依赖真实 DOM 量测,vitest 浏览器模式测)。资源引用在 view-model 里是**不透明 `ResourceRef`**,由消费方注入的 `ResourceResolver` 解析成 URL(web 走 HTTP、desktop 走 IPC),这是 web/desktop 的唯一耦合点。标注 PNG 在契约里保留 `1x`/`2x` 两档预览引用,`AnnotationTile` 用 `src` + `srcSet` 实现浏览器 LOD。区域虚拟化用 React Flow `onlyRenderVisibleElements` 只挂载视口内 tile、离屏卸载。

**Tech Stack:** TypeScript ESM (Node ≥22, `moduleResolution: Bundler`)、React 19、React Flow `@xyflow/react` v12 (MIT)、Vite 7 + `@vitejs/plugin-react`、Vitest 4 浏览器模式(Playwright provider, chromium headless)。

---

## ⚠️ 执行期前置核对(Context7 当前不可用,务必在动手前补做)

本 plan 写作时 Context7 MCP 返回 `Invalid API key`,无法核对版本敏感 API。以下标记 `UNCONFIRMED` 的接口**必须在对应 task 动手前用 Context7 核对**(`resolve-library-id` → `query-docs`),若 Context7 仍不可用则改用官方文档/源码,并把结论记进 `CONTINUITY.md`:

1. **`@xyflow/react` v12 API**(Task 6/8/9/12):`ReactFlow`/`ReactFlowProvider`/`useReactFlow`、`nodeTypes` 自定义节点、`onlyRenderVisibleElements` prop、`setCenter(x,y,{zoom})`、`Node`/`NodeProps` 类型、CSS 引入路径 `@xyflow/react/dist/style.css`、license(确认 MIT)。**不要在生产 Canvas 默认启用 `fitView`**,否则会把全图缩进视口并破坏离屏卸载语义。
2. **Vitest 4 browser mode**(Task 5):`test.browser.enabled`、`provider: "playwright"`、`instances`/`browser` 配置键、`test.projects`(替代旧 workspace 文件)的精确语法。
3. **Playwright provider 安装**(Task 5):`@vitest/browser` 与 `playwright` 版本、`npx playwright install chromium` 是否必要。

**纪律提醒(来自踩过的坑):** 串行执行,一个 task 完整落盘 + 测试绿 + 提交后再开下一个;**绝不并行派发多个 implementer**;subagent prompt 里**不要用没核实过的 commit SHA**。

---

## File Structure

新建包 `packages/viewer/`,职责单一、文件聚焦:

| 文件 | 职责 |
|---|---|
| `packages/viewer/package.json` | 包清单:`@xenonbyte/forma-viewer`,private,源码 exports,deps/devDeps,scripts |
| `packages/viewer/tsconfig.json` | TS 配置(Bundler + DOM + jsx,参照 web) |
| `packages/viewer/vite.config.ts` | Vite 插件(react),供 vitest 浏览器模式复用 transform |
| `packages/viewer/README.md` | view-model 契约文档 + 消费方接入说明 |
| `packages/viewer/src/index.ts` | 包公开出口(re-export 类型 + 组件) |
| `packages/viewer/src/model.ts` | 规范化 view-model 类型契约(纯类型,无运行时) |
| `packages/viewer/src/normalize.ts` | `buildViewerModel()`:中性 artifact 输入 → `ViewerModel`(分组/排序) |
| `packages/viewer/src/layout.ts` | `layoutTiles()`:tile 在无限画布上的 x/y 网格定位(纯数学) |
| `packages/viewer/src/tiles/DesignTile.tsx` | 设计画布 tile:`<iframe sandbox>` 渲静态 HTML |
| `packages/viewer/src/tiles/AnnotationTile.tsx` | 标注画布 tile:`<img>` 渲 PNG |
| `packages/viewer/src/Canvas.tsx` | React Flow 无限画布外壳(自定义节点 + 区域虚拟化 + 定位) |
| `packages/viewer/src/DesignList.tsx` | 左侧设计稿列表(按 page→variant,点击定位) |
| `packages/viewer/src/AnnotationSlot.tsx` | 右侧标注信息栏(预留空占位) |
| `packages/viewer/src/Viewer.tsx` | 顶层组件:左/中/右三栏装配 + 画布模式切换 |
| `packages/viewer/tests/normalize.test.ts` | 纯逻辑 node 测试(normalize) |
| `packages/viewer/tests/layout.test.ts` | 纯逻辑 node 测试(layout) |
| `packages/viewer/src/*.browser.test.tsx` | 组件浏览器模式测试(与被测组件同目录) |

**根级改动:**
- `vitest.config.ts`:改为 `projects` 结构,新增 viewer 浏览器项目 + viewer node 项目,保留现有 node/happy-dom 测试不变。
- `vitest.config.ts` 的 `workspaceAliases`:新增 `@xenonbyte/forma-viewer` → `packages/viewer/src/index.ts`。

**消费模型说明(写进 README,本期不改 web/desktop——那是 P8/P9):** viewer 通过 `exports` 指向 `./src/index.ts`,web/desktop 的 Vite 直接打包源码;`pnpm build` 对 viewer 无 emit(无 `build` script,被 `--if-present` 跳过);`pnpm typecheck` 跑 viewer 的 `tsc --noEmit`。

---

## Task 1: Scaffold `@xenonbyte/forma-viewer` 包骨架

**Files:**
- Create: `packages/viewer/package.json`
- Create: `packages/viewer/tsconfig.json`
- Create: `packages/viewer/vite.config.ts`
- Create: `packages/viewer/src/index.ts`
- Create: `packages/viewer/README.md`
- Modify: `vitest.config.ts`(新增 alias)

- [ ] **Step 1: 写 `package.json`**

```json
{
  "name": "@xenonbyte/forma-viewer",
  "version": "0.1.8",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "react": "^18.3.1 || ^19.0.0",
    "react-dom": "^18.3.1 || ^19.0.0"
  },
  "dependencies": {
    "@xyflow/react": "^12.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^7.0.0"
  }
}
```

> **peerDeps 跨 React 18/19:** desktop 当前是 React **18.3**,web 是 React **19**(已核实两个 package.json)。共享包用 `^18.3.1 || ^19.0.0` 的 peer 范围,确保 P9 desktop 接入不产生版本冲突;devDeps 用 19 本地开发/测试。
> `@vitest/browser` + `playwright` 在 Task 5 加入(那时才需要,避免一次性引入未用依赖)。`@xyflow/react` 版本范围执行期 Context7 核对后再定死。`@xyflow/react` 对 React 18/19 的 peer 兼容性执行期一并核对。

- [ ] **Step 2: 写 `tsconfig.json`**(必须 `extends ../../tsconfig.base.json`,与 `packages/web/tsconfig.json` 同惯例)

> ⚠️ `tsconfig.base.json` 启用了 `verbatimModuleSyntax: true` + `moduleResolution: bundler`。因此:相对 import **必须带 `.js` 后缀**(如 `./model.js`),类型导入/导出**必须用 `import type` / `export type`**。本 plan 全程已遵守,实现时勿改成无后缀写法。

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vitest/globals", "vite/client"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "tests/**/*.ts", "vite.config.ts"]
}
```

- [ ] **Step 3: 写 `vite.config.ts`**

```ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [react()]
});
```

- [ ] **Step 4: 写 `src/index.ts`**(占位出口,后续 task 逐步填充)

```ts
export const FORMA_VIEWER_PACKAGE = "@xenonbyte/forma-viewer";
```

- [ ] **Step 5: 写 `README.md`**(契约文档骨架,后续 task 补充契约细节)

```markdown
# @xenonbyte/forma-viewer

Forma 共享只读查看器:无限画布外壳(左 设计稿列表 / 中 画布 / 右 标注 slot),
设计画布渲染静态 HTML(`<iframe sandbox>` 禁脚本),标注画布渲染 PNG。
纯展示、数据驱动;web(HTTP)/desktop(IPC) 经同一 view-model 契约 + 注入式
资源解析器同源消费。

## 消费模型
- 通过 `exports` 暴露 `./src/index.ts`,消费方(web/desktop,均 Vite)直接打包源码。
- 无独立 build emit;类型检查走 `pnpm --filter @xenonbyte/forma-viewer typecheck`。

## 契约
详见 `src/model.ts`(后续 task 填充本节)。
```

- [ ] **Step 6: 在根 `vitest.config.ts` 的 `workspaceAliases` 增加 viewer alias**

把:
```ts
  "@xenonbyte/forma-web": new URL("./packages/web/src/App.tsx", import.meta.url).pathname
```
改为:
```ts
  "@xenonbyte/forma-web": new URL("./packages/web/src/App.tsx", import.meta.url).pathname,
  "@xenonbyte/forma-viewer": new URL("./packages/viewer/src/index.ts", import.meta.url).pathname
```

- [ ] **Step 7: 安装依赖并验证包被工作区识别**

Run: `pnpm install`
Expected: 安装成功,`@xenonbyte/forma-viewer` 出现在 workspace,无 peerDep error。

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck`
Expected: PASS(`tsc --noEmit` 干净,exit 0)。

- [ ] **Step 8: 验证现有全量测试不被破坏**

Run: `pnpm test`
Expected: 仍 `934 passed`(viewer 此刻无测试文件,`passWithNoTests` 兜底)。

- [ ] **Step 9: Commit**

```bash
git add packages/viewer/package.json packages/viewer/tsconfig.json packages/viewer/vite.config.ts packages/viewer/src/index.ts packages/viewer/README.md vitest.config.ts pnpm-lock.yaml
git commit -m "feat(p7): scaffold @xenonbyte/forma-viewer package

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: view-model 数据契约类型

**Files:**
- Create: `packages/viewer/src/model.ts`
- Modify: `packages/viewer/src/index.ts`
- Test: 类型契约由后续 normalize/layout 测试间接覆盖;本 task 仅定义类型 + 一个 type-guard 并为 guard 写 node 测试。
- Test: `packages/viewer/tests/model.test.ts`

- [ ] **Step 1: 写失败测试 `packages/viewer/tests/model.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { isDesignTile, type ViewerTile } from "@xenonbyte/forma-viewer";

describe("isDesignTile", () => {
  const base: Omit<ViewerTile, "kind"> = {
    id: "a:1:default",
    pageId: "login",
    pageName: "登录页",
    variant: "default",
    title: "登录页",
    version: 1,
    width: 1280,
    height: 800,
    htmlBundle: { artifactId: "a", version: 1, kind: "bundle" },
    previewImages: {
      "1x": { artifactId: "a", version: 1, kind: "preview", density: "1x" },
      "2x": { artifactId: "a", version: 1, kind: "preview", density: "2x" }
    }
  };

  it("returns true for design tiles", () => {
    expect(isDesignTile({ ...base, kind: "design-page" })).toBe(true);
  });

  it("returns false for component-library tiles", () => {
    expect(isDesignTile({ ...base, kind: "component-library" })).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test packages/viewer/tests/model.test.ts`
Expected: FAIL — `isDesignTile`/`ViewerTile` 未从包导出。

- [ ] **Step 3: 写 `packages/viewer/src/model.ts`**

```ts
/**
 * Forma viewer 规范化数据契约。
 *
 * 这是 web(HTTP)/desktop(IPC) 的唯一耦合点:两端各自把自身数据源映射成
 * 这套与传输无关的 view-model,再注入资源解析器。契约变更必须同步两端 + 测试。
 */

/** artifact 的产出种类。 */
export type ArtifactKind = "design-page" | "component-library";

/** 画布模式:设计画布渲 HTML,标注画布渲 PNG。共用同一外壳。 */
export type CanvasMode = "design" | "annotation";

/** 查看器入口范围:需求入口铺全需求所有页,页面入口只铺该页的 variant。 */
export type ViewerEntry = "requirement" | "page";

/** 标注 PNG 预览密度。P7 contract 显式保留 1x/2x LOD。 */
export type PreviewDensity = "1x" | "2x";

/**
 * 不透明资源引用。viewer 不知道也不关心真实 URL;由消费方注入的
 * ResourceResolver 解析(web→HTTP URL,desktop→IPC/app URL)。
 */
export interface ResourceRef {
  artifactId: string;
  version: number;
  /** bundle=自包含 HTML 入口;preview=PNG 预览;asset=bundle 内子资源(需 path)。 */
  kind: "bundle" | "preview" | "asset";
  /** kind="preview" 时的 PNG density;AnnotationTile 使用 1x/2x 生成 src/srcSet。 */
  density?: PreviewDensity;
  /** kind="asset" 时的 bundle 内相对路径。 */
  path?: string;
}

/** 标注画布 PNG 资源。1x/2x 都必须存在,对应 Phase 7 LOD contract。 */
export interface PreviewImageRefs {
  "1x": ResourceRef;
  "2x": ResourceRef;
}

/** 一个画布瓦片 = 一个 artifact 版本,可被两种渲染器渲染。 */
export interface ViewerTile {
  /** 稳定唯一 id,形如 `${artifactId}:${version}:${variant}`。 */
  id: string;
  kind: ArtifactKind;
  /** 分组键:左列表按 page 分组。 */
  pageId: string;
  pageName: string;
  /** 同页的设计变体,如 "default"。 */
  variant: string;
  title: string;
  version: number;
  /** 内在画布尺寸(布局用,单位 px)。 */
  width: number;
  height: number;
  /** 设计画布用:自包含 HTML bundle 引用。 */
  htmlBundle: ResourceRef;
  /** 标注画布用:PNG 预览引用,保留 1x/2x LOD。 */
  previewImages: PreviewImageRefs;
}

/** 左列表分组(一页一组,组内按 variant 排序)。 */
export interface ViewerGroup {
  pageId: string;
  pageName: string;
  /** 组内 tile id,按 variant 顺序。 */
  tileIds: string[];
}

/** 已定位的 tile(带画布坐标)。 */
export interface PositionedTile extends ViewerTile {
  x: number;
  y: number;
}

/** viewer 的完整渲染输入。 */
export interface ViewerModel {
  entry: ViewerEntry;
  tiles: PositionedTile[];
  groups: ViewerGroup[];
}

/** 消费方注入:把不透明 ResourceRef 解析成可用于 iframe/img 的 URL。 */
export interface ResourceResolver {
  resolve(ref: ResourceRef): string;
}

/** 设计稿 tile 判定。 */
export function isDesignTile(tile: ViewerTile): boolean {
  return tile.kind === "design-page";
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出类型与 guard**

把 `src/index.ts` 改为:
```ts
export const FORMA_VIEWER_PACKAGE = "@xenonbyte/forma-viewer";

export type {
  ArtifactKind,
  CanvasMode,
  ViewerEntry,
  PreviewDensity,
  ResourceRef,
  PreviewImageRefs,
  ViewerTile,
  ViewerGroup,
  PositionedTile,
  ViewerModel,
  ResourceResolver
} from "./model.js";
export { isDesignTile } from "./model.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test packages/viewer/tests/model.test.ts`
Expected: PASS(2 tests)。

- [ ] **Step 6: 类型检查**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/viewer/src/model.ts packages/viewer/src/index.ts packages/viewer/tests/model.test.ts
git commit -m "feat(p7): viewer view-model data contract types

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: view-model 规范化 `buildViewerModel()`

**Files:**
- Create: `packages/viewer/src/normalize.ts`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/tests/normalize.test.ts`

- [ ] **Step 1: 写失败测试 `packages/viewer/tests/normalize.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { buildViewerModel, type NormalizeArtifactInput } from "@xenonbyte/forma-viewer";

const inputs: NormalizeArtifactInput[] = [
  {
    artifactId: "art-login",
    kind: "design-page",
    pageId: "login",
    pageName: "登录页",
    variant: "default",
    title: "登录页 默认",
    version: 2,
    width: 1280,
    height: 800
  },
  {
    artifactId: "art-login",
    kind: "design-page",
    pageId: "login",
    pageName: "登录页",
    variant: "wide",
    title: "登录页 宽屏",
    version: 2,
    width: 1440,
    height: 900
  },
  {
    artifactId: "art-home",
    kind: "design-page",
    pageId: "home",
    pageName: "首页",
    variant: "default",
    title: "首页",
    version: 1,
    width: 1280,
    height: 800
  }
];

describe("buildViewerModel", () => {
  it("groups tiles by page and orders variants within a group", () => {
    const model = buildViewerModel({ entry: "requirement", artifacts: inputs });
    expect(model.groups.map((g) => g.pageId)).toEqual(["login", "home"]);
    const login = model.groups.find((g) => g.pageId === "login");
    expect(login?.tileIds).toEqual(["art-login:2:default", "art-login:2:wide"]);
  });

  it("builds opaque resource refs for html bundle and preview 1x/2x LOD", () => {
    const model = buildViewerModel({ entry: "requirement", artifacts: inputs });
    const tile = model.tiles.find((t) => t.id === "art-home:1:default");
    expect(tile?.htmlBundle).toEqual({ artifactId: "art-home", version: 1, kind: "bundle" });
    expect(tile?.previewImages).toEqual({
      "1x": { artifactId: "art-home", version: 1, kind: "preview", density: "1x" },
      "2x": { artifactId: "art-home", version: 1, kind: "preview", density: "2x" }
    });
  });

  it("assigns non-overlapping canvas positions", () => {
    const model = buildViewerModel({ entry: "requirement", artifacts: inputs });
    const positions = model.tiles.map((t) => `${t.x},${t.y}`);
    expect(new Set(positions).size).toBe(model.tiles.length);
  });

  it("carries the entry scope through", () => {
    const model = buildViewerModel({ entry: "page", artifacts: [inputs[0]] });
    expect(model.entry).toBe("page");
    expect(model.tiles).toHaveLength(1);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test packages/viewer/tests/normalize.test.ts`
Expected: FAIL — `buildViewerModel`/`NormalizeArtifactInput` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/normalize.ts`**

```ts
import { layoutTiles } from "./layout.js";
import type { ArtifactKind, ViewerEntry, ViewerGroup, ViewerModel, ViewerTile } from "./model.js";

/** 传输中性的单个 artifact 输入(web/desktop 各自映射到此)。 */
export interface NormalizeArtifactInput {
  artifactId: string;
  kind: ArtifactKind;
  pageId: string;
  pageName: string;
  variant: string;
  title: string;
  version: number;
  width: number;
  height: number;
}

export interface BuildViewerModelInput {
  entry: ViewerEntry;
  artifacts: NormalizeArtifactInput[];
}

function tileId(a: NormalizeArtifactInput): string {
  return `${a.artifactId}:${a.version}:${a.variant}`;
}

/**
 * 把中性 artifact 列表规范化为 ViewerModel:
 * - 按首次出现顺序保持页分组顺序;组内按 variant 字典序排序(default 优先)。
 * - 为每个 tile 生成不透明 htmlBundle / previewImages(1x/2x) 引用。
 * - 调 layoutTiles 计算非重叠画布坐标。
 */
export function buildViewerModel(input: BuildViewerModelInput): ViewerModel {
  const tiles: ViewerTile[] = input.artifacts.map((a) => ({
    id: tileId(a),
    kind: a.kind,
    pageId: a.pageId,
    pageName: a.pageName,
    variant: a.variant,
    title: a.title,
    version: a.version,
    width: a.width,
    height: a.height,
    htmlBundle: { artifactId: a.artifactId, version: a.version, kind: "bundle" },
    previewImages: buildPreviewRefs(a)
  }));

  const groups = buildGroups(tiles);
  const positioned = layoutTiles(tiles, groups);

  return { entry: input.entry, tiles: positioned, groups };
}

function buildPreviewRefs(a: NormalizeArtifactInput): ViewerTile["previewImages"] {
  return {
    "1x": { artifactId: a.artifactId, version: a.version, kind: "preview", density: "1x" },
    "2x": { artifactId: a.artifactId, version: a.version, kind: "preview", density: "2x" }
  };
}

function buildGroups(tiles: ViewerTile[]): ViewerGroup[] {
  const order: string[] = [];
  const byPage = new Map<string, ViewerTile[]>();
  for (const tile of tiles) {
    if (!byPage.has(tile.pageId)) {
      byPage.set(tile.pageId, []);
      order.push(tile.pageId);
    }
    byPage.get(tile.pageId)!.push(tile);
  }

  return order.map((pageId) => {
    const pageTiles = byPage.get(pageId)!;
    const sorted = [...pageTiles].sort(compareVariant);
    return {
      pageId,
      pageName: pageTiles[0].pageName,
      tileIds: sorted.map((t) => t.id)
    };
  });
}

/** "default" 永远排首位,其余按字典序。 */
function compareVariant(a: ViewerTile, b: ViewerTile): number {
  if (a.variant === b.variant) return 0;
  if (a.variant === "default") return -1;
  if (b.variant === "default") return 1;
  return a.variant < b.variant ? -1 : 1;
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

在 `src/index.ts` 末尾追加:
```ts
export { buildViewerModel } from "./normalize.js";
export type { NormalizeArtifactInput, BuildViewerModelInput } from "./normalize.js";
```

- [ ] **Step 5: 跑测试**(此时 `layout.ts` 尚未建,会因 import 失败)

Run: `pnpm test packages/viewer/tests/normalize.test.ts`
Expected: FAIL — `./layout.js` 不存在。这是预期的;Task 4 建 `layout.ts` 后本测试转绿。**本 task 先提交 normalize + 它的测试,layout 在 Task 4 落地。**

> 注意:为保持每个 task 可独立提交且测试绿,**调整顺序**——先做 Task 4(layout,纯函数无依赖),再回到 Task 3 让 normalize 测试转绿。执行者请按 **Task 4 → Task 3** 顺序做这两个;若已先写了 Task 3 代码,提交前确保 Task 4 也已就绪。下面 Task 4 紧接给出 layout。

- [ ] **Step 6: (在 Task 4 完成后)跑测试确认通过**

Run: `pnpm test packages/viewer/tests/normalize.test.ts`
Expected: PASS(4 tests)。

- [ ] **Step 7: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/normalize.ts packages/viewer/src/index.ts packages/viewer/tests/normalize.test.ts
git commit -m "feat(p7): viewer model normalization (grouping + opaque refs)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: tile 画布布局 `layoutTiles()`(纯数学,先于 Task 3 落地)

**Files:**
- Create: `packages/viewer/src/layout.ts`
- Test: `packages/viewer/tests/layout.test.ts`

> 执行顺序:**本 task 先于 Task 3 的最终提交**。layout 是无依赖纯函数,先建它,Task 3 的 normalize 才能 import 成功。

- [ ] **Step 1: 写失败测试 `packages/viewer/tests/layout.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { layoutTiles, TILE_GAP } from "@xenonbyte/forma-viewer";
import type { ViewerGroup, ViewerTile } from "@xenonbyte/forma-viewer";

function tile(id: string, pageId: string, variant: string, width = 1000, height = 600): ViewerTile {
  return {
    id, kind: "design-page", pageId, pageName: pageId, variant,
    title: id, version: 1, width, height,
    htmlBundle: { artifactId: id, version: 1, kind: "bundle" },
    previewImages: {
      "1x": { artifactId: id, version: 1, kind: "preview", density: "1x" },
      "2x": { artifactId: id, version: 1, kind: "preview", density: "2x" }
    }
  };
}

describe("layoutTiles", () => {
  const tiles: ViewerTile[] = [
    tile("a", "login", "default"),
    tile("b", "login", "wide"),
    tile("c", "home", "default")
  ];
  const groups: ViewerGroup[] = [
    { pageId: "login", pageName: "login", tileIds: ["a", "b"] },
    { pageId: "home", pageName: "home", tileIds: ["c"] }
  ];

  it("places each group on its own row", () => {
    const positioned = layoutTiles(tiles, groups);
    const a = positioned.find((t) => t.id === "a")!;
    const b = positioned.find((t) => t.id === "b")!;
    const c = positioned.find((t) => t.id === "c")!;
    expect(a.y).toBe(b.y); // 同组同行
    expect(c.y).toBeGreaterThan(a.y); // 下一组在下一行
  });

  it("lays out variants left-to-right within a group with a gap", () => {
    const positioned = layoutTiles(tiles, groups);
    const a = positioned.find((t) => t.id === "a")!;
    const b = positioned.find((t) => t.id === "b")!;
    expect(a.x).toBe(0);
    expect(b.x).toBe(a.width + TILE_GAP);
  });

  it("returns one positioned tile per input tile", () => {
    expect(layoutTiles(tiles, groups)).toHaveLength(3);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test packages/viewer/tests/layout.test.ts`
Expected: FAIL — `layoutTiles`/`TILE_GAP` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/layout.ts`**

```ts
import type { PositionedTile, ViewerGroup, ViewerTile } from "./model.js";

/** tile 之间的画布间距(px)。 */
export const TILE_GAP = 80;

/**
 * 把 tiles 按 group 布到无限画布:
 * - 每个 group 一行(page 一行)。
 * - 行内 tile 从左到右按 group.tileIds 顺序排,水平间距 TILE_GAP。
 * - 行高 = 该行最高 tile + TILE_GAP;行 y 依次累加。
 */
export function layoutTiles(tiles: ViewerTile[], groups: ViewerGroup[]): PositionedTile[] {
  const byId = new Map(tiles.map((t) => [t.id, t]));
  const positioned: PositionedTile[] = [];
  let rowY = 0;

  for (const group of groups) {
    let cursorX = 0;
    let rowHeight = 0;
    for (const tileId of group.tileIds) {
      const tile = byId.get(tileId);
      if (!tile) continue;
      positioned.push({ ...tile, x: cursorX, y: rowY });
      cursorX += tile.width + TILE_GAP;
      rowHeight = Math.max(rowHeight, tile.height);
    }
    rowY += rowHeight + TILE_GAP;
  }

  return positioned;
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

在 `src/index.ts` 追加:
```ts
export { layoutTiles, TILE_GAP } from "./layout.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test packages/viewer/tests/layout.test.ts`
Expected: PASS(3 tests)。

- [ ] **Step 6: 跑 normalize 测试确认转绿(Task 3 依赖本 task)**

Run: `pnpm test packages/viewer/tests/normalize.test.ts`
Expected: PASS(4 tests)。

- [ ] **Step 7: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/layout.ts packages/viewer/src/index.ts packages/viewer/tests/layout.test.ts
git commit -m "feat(p7): viewer tile canvas layout

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 建立 Vitest 浏览器模式测试基建(关键 gate)

**⚠️ 执行期先用 Context7 核对 Vitest 4 browser/projects + Playwright provider API(见顶部前置核对 #2/#3)。**

**Files:**
- Modify: `packages/viewer/package.json`(加 `@vitest/browser` + `playwright`)
- Modify: `vitest.config.ts`(改为 `projects` 结构)
- Create: `packages/viewer/src/smoke.browser.test.tsx`(基建验证用最小浏览器测试)

- [ ] **Step 1: 加浏览器测试依赖到 `packages/viewer/package.json` 的 devDependencies**

在 devDependencies 增加(精确版本执行期核对):
```json
    "@vitest/browser": "^4.0.7",
    "playwright": "^1.50.0"
```

Run: `pnpm install`
Expected: 安装成功。
Run(如核对结论要求): `npx playwright install chromium`
Expected: chromium 就绪。

- [ ] **Step 2: 把根 `vitest.config.ts` 改为 `projects` 结构**

将现有:
```ts
export default defineConfig({
  resolve: {
    alias: workspaceAliases
  },
  test: {
    globals: true,
    environment: "node",
    include: ["packages/*/tests/**/*.test.ts", "packages/web/src/**/*.test.ts", "packages/web/src/**/*.test.tsx"],
    passWithNoTests: true
  }
});
```
改为(精确 `projects`/`browser` 键执行期 Context7 核对后定稿):
```ts
export default defineConfig({
  resolve: {
    alias: workspaceAliases
  },
  test: {
    globals: true,
    passWithNoTests: true,
    projects: [
      {
        resolve: { alias: workspaceAliases },
        test: {
          name: "unit",
          globals: true,
          environment: "node",
          include: [
            "packages/*/tests/**/*.test.ts",
            "packages/web/src/**/*.test.ts",
            "packages/web/src/**/*.test.tsx"
          ],
          // viewer 的浏览器测试由下面的 viewer 项目跑,这里排除
          exclude: ["**/*.browser.test.tsx", "**/node_modules/**"],
          passWithNoTests: true
        }
      },
      {
        resolve: { alias: workspaceAliases },
        plugins: [],
        test: {
          name: "viewer",
          globals: true,
          include: ["packages/viewer/src/**/*.browser.test.tsx"],
          passWithNoTests: true,
          browser: {
            enabled: true,
            provider: "playwright",
            headless: true,
            instances: [{ browser: "chromium" }]
          }
        }
      }
    ]
  }
});
```

> 说明:
> - viewer 的**纯逻辑** node 测试在 `packages/viewer/tests/**/*.test.ts`,被 `unit` 项目的 `packages/*/tests/**` glob 覆盖(node 环境),无需改动。
> - viewer 的**组件**测试统一命名 `*.browser.test.tsx` 放在 `packages/viewer/src/` 下,由 `viewer` 浏览器项目跑。
> - `unit` 项目 `exclude` 掉 `*.browser.test.tsx`,避免 node 环境误跑浏览器测试。
> - viewer 浏览器项目需要 React transform——执行期确认是否要在该项目 `plugins` 里加 `@vitejs/plugin-react`(若 vitest 不自动应用 `packages/viewer/vite.config.ts`,则显式加;核对后定)。

- [ ] **Step 3: 写最小浏览器 smoke 测试 `packages/viewer/src/smoke.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;

const roots: Root[] = [];
const containers: HTMLElement[] = [];

function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => {
    root.render(ui);
  });
  roots.push(root);
  containers.push(container);
  return container;
}

afterEach(() => {
  for (const root of roots.splice(0)) {
    act(() => root.unmount());
  }
  for (const container of containers.splice(0)) {
    container.remove();
  }
});

describe("browser-mode smoke", () => {
  it("mounts a React component into a real DOM with layout measurement", () => {
    const container = render(<div style={{ width: 200, height: 100 }}>hello</div>);
    const el = container.firstElementChild as HTMLElement;
    expect(el.textContent).toBe("hello");
    // 真实浏览器才有非零量测;happy-dom/jsdom 通常返回 0,以此证明确在真实浏览器跑。
    expect(el.getBoundingClientRect().width).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 4: 跑 viewer 浏览器项目,确认 smoke 通过**

Run: `pnpm test --project viewer`(或执行期核对的等效选择器)
Expected: PASS(1 test),且 `getBoundingClientRect().width > 0` 证明确在真实浏览器。

- [ ] **Step 5: 跑全量,确认现有 934 测试 + viewer 全绿且无环境串扰**

Run: `pnpm test`
Expected: 全绿 = 934(既有)+ 之前 task 的 viewer node 测试(model/normalize/layout 共 9)+ smoke 1。**逐项确认 `Test Files` 数与 `Tests` 数都增长且 0 failed。**

- [ ] **Step 6: Commit**

```bash
git add packages/viewer/package.json packages/viewer/src/smoke.browser.test.tsx vitest.config.ts pnpm-lock.yaml
git commit -m "test(p7): vitest browser-mode infra via projects (playwright/chromium)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 设计画布 tile 渲染器 `DesignTile`(iframe sandbox 禁脚本)

**Files:**
- Create: `packages/viewer/src/tiles/DesignTile.tsx`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/src/tiles/DesignTile.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/tiles/DesignTile.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { DesignTile } from "@xenonbyte/forma-viewer";
import type { PositionedTile, ResourceResolver } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});

const tile: PositionedTile = {
  id: "a:1:default", kind: "design-page", pageId: "login", pageName: "登录页",
  variant: "default", title: "登录页", version: 1, width: 800, height: 600, x: 0, y: 0,
  htmlBundle: { artifactId: "a", version: 1, kind: "bundle" },
  previewImages: {
    "1x": { artifactId: "a", version: 1, kind: "preview", density: "1x" },
    "2x": { artifactId: "a", version: 1, kind: "preview", density: "2x" }
  }
};
const resolver: ResourceResolver = {
  resolve: (ref) =>
    `https://example.test/${ref.artifactId}/v${ref.version}/${ref.kind}${ref.density ? `-${ref.density}` : ""}`
};

describe("DesignTile", () => {
  it("renders an iframe whose src is the resolved html bundle", () => {
    const container = render(<DesignTile tile={tile} resolver={resolver} />);
    const iframe = container.querySelector("iframe");
    expect(iframe).not.toBeNull();
    expect(iframe!.getAttribute("src")).toBe("https://example.test/a/v1/bundle");
  });

  it("sandboxes the iframe without allow-scripts", () => {
    const container = render(<DesignTile tile={tile} resolver={resolver} />);
    const sandbox = container.querySelector("iframe")!.getAttribute("sandbox");
    expect(sandbox).not.toBeNull();
    expect(sandbox!).not.toContain("allow-scripts");
  });

  it("sizes the iframe to the tile intrinsic dimensions", () => {
    const container = render(<DesignTile tile={tile} resolver={resolver} />);
    const iframe = container.querySelector("iframe")!;
    expect(iframe.getAttribute("width")).toBe("800");
    expect(iframe.getAttribute("height")).toBe("600");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/tiles/DesignTile.browser.test.tsx`
Expected: FAIL — `DesignTile` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/tiles/DesignTile.tsx`**

```tsx
import type { PositionedTile, ResourceResolver } from "../model.js";

export interface DesignTileProps {
  tile: PositionedTile;
  resolver: ResourceResolver;
}

/**
 * 设计画布瓦片:在沙箱 iframe 里渲染自包含静态 HTML。
 * sandbox 明确不含 allow-scripts —— 设计稿是纯静态产物,禁止脚本执行。
 */
export function DesignTile({ tile, resolver }: DesignTileProps): React.ReactElement {
  const src = resolver.resolve(tile.htmlBundle);
  return (
    <iframe
      title={tile.title}
      src={src}
      width={tile.width}
      height={tile.height}
      // 允许同源资源读取与表单展示,但不含 allow-scripts。
      sandbox="allow-same-origin allow-forms"
      style={{ border: "none", display: "block", background: "#fff" }}
    />
  );
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

追加:
```ts
export { DesignTile } from "./tiles/DesignTile.js";
export type { DesignTileProps } from "./tiles/DesignTile.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test --project viewer packages/viewer/src/tiles/DesignTile.browser.test.tsx`
Expected: PASS(3 tests)。

- [ ] **Step 6: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/tiles/DesignTile.tsx packages/viewer/src/index.ts packages/viewer/src/tiles/DesignTile.browser.test.tsx
git commit -m "feat(p7): DesignTile static-HTML iframe renderer (sandbox, no scripts)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 标注画布 tile 渲染器 `AnnotationTile`(PNG 1x/2x LOD)

**Files:**
- Create: `packages/viewer/src/tiles/AnnotationTile.tsx`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/src/tiles/AnnotationTile.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/tiles/AnnotationTile.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { AnnotationTile } from "@xenonbyte/forma-viewer";
import type { PositionedTile, ResourceResolver } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});

const tile: PositionedTile = {
  id: "a:1:default", kind: "design-page", pageId: "login", pageName: "登录页",
  variant: "default", title: "登录页", version: 1, width: 800, height: 600, x: 0, y: 0,
  htmlBundle: { artifactId: "a", version: 1, kind: "bundle" },
  previewImages: {
    "1x": { artifactId: "a", version: 1, kind: "preview", density: "1x" },
    "2x": { artifactId: "a", version: 1, kind: "preview", density: "2x" }
  }
};
const resolver: ResourceResolver = {
  resolve: (ref) =>
    `https://example.test/${ref.artifactId}/v${ref.version}/${ref.kind}${ref.density ? `-${ref.density}` : ""}`
};

describe("AnnotationTile", () => {
  it("renders an img whose src is the resolved 1x preview png", () => {
    const container = render(<AnnotationTile tile={tile} resolver={resolver} />);
    const img = container.querySelector("img");
    expect(img).not.toBeNull();
    expect(img!.getAttribute("src")).toBe("https://example.test/a/v1/preview-1x");
  });

  it("renders a 2x preview srcSet when available", () => {
    const container = render(<AnnotationTile tile={tile} resolver={resolver} />);
    expect(container.querySelector("img")!.getAttribute("srcset")).toBe(
      "https://example.test/a/v1/preview-2x 2x"
    );
  });

  it("uses the tile title as alt text", () => {
    const container = render(<AnnotationTile tile={tile} resolver={resolver} />);
    expect(container.querySelector("img")!.getAttribute("alt")).toBe("登录页");
  });

  it("sizes the image to the tile intrinsic dimensions", () => {
    const container = render(<AnnotationTile tile={tile} resolver={resolver} />);
    const img = container.querySelector("img")!;
    expect(img.getAttribute("width")).toBe("800");
    expect(img.getAttribute("height")).toBe("600");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/tiles/AnnotationTile.browser.test.tsx`
Expected: FAIL — `AnnotationTile` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/tiles/AnnotationTile.tsx`**

```tsx
import type { PositionedTile, ResourceResolver } from "../model.js";

export interface AnnotationTileProps {
  tile: PositionedTile;
  resolver: ResourceResolver;
}

/**
 * 标注画布瓦片:渲染该 artifact 版本的 PNG 预览,用 src/srcSet 保留 1x/2x LOD。
 * 标注内容(右侧 slot)本期不实现,这里只是 PNG 底图。
 */
export function AnnotationTile({ tile, resolver }: AnnotationTileProps): React.ReactElement {
  const src = resolver.resolve(tile.previewImages["1x"]);
  const srcSet = `${resolver.resolve(tile.previewImages["2x"])} 2x`;
  return (
    <img
      src={src}
      srcSet={srcSet}
      alt={tile.title}
      width={tile.width}
      height={tile.height}
      style={{ display: "block", background: "#fff" }}
    />
  );
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

追加:
```ts
export { AnnotationTile } from "./tiles/AnnotationTile.js";
export type { AnnotationTileProps } from "./tiles/AnnotationTile.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test --project viewer packages/viewer/src/tiles/AnnotationTile.browser.test.tsx`
Expected: PASS(4 tests,含 2x `srcSet`)。

- [ ] **Step 6: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/tiles/AnnotationTile.tsx packages/viewer/src/index.ts packages/viewer/src/tiles/AnnotationTile.browser.test.tsx
git commit -m "feat(p7): AnnotationTile PNG renderer

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: React Flow 无限画布外壳 `Canvas`

**⚠️ 执行期先用 Context7 核对 `@xyflow/react` v12 API + license(顶部前置 #1)。下方代码基于 v12 习惯写法,标 `UNCONFIRMED`,核对后按真实 API 调整 import/prop 名。**

**Files:**
- Create: `packages/viewer/src/Canvas.tsx`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/src/Canvas.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/Canvas.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { Canvas, buildViewerModel } from "@xenonbyte/forma-viewer";
import type { NormalizeArtifactInput, ResourceResolver } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  container.style.width = "1024px";
  container.style.height = "768px";
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});

const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));

const artifacts: NormalizeArtifactInput[] = [
  { artifactId: "a", kind: "design-page", pageId: "login", pageName: "登录页", variant: "default", title: "登录页", version: 1, width: 600, height: 400 },
  { artifactId: "b", kind: "design-page", pageId: "home", pageName: "首页", variant: "default", title: "首页", version: 1, width: 600, height: 400 }
];
const resolver: ResourceResolver = { resolve: (ref) => `https://example.test/${ref.artifactId}/${ref.kind}` };

describe("Canvas", () => {
  it("renders a React Flow canvas with one node per tile in design mode", async () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<Canvas model={model} mode="design" resolver={resolver} />);
    await act(async () => { await sleep(50); });
    // React Flow 给每个 node 渲染一个带 data-id 的 .react-flow__node 元素
    const nodes = container.querySelectorAll(".react-flow__node");
    expect(nodes.length).toBeGreaterThanOrEqual(1);
    // 设计模式下应出现 iframe(至少视口内的那张)
    expect(container.querySelector("iframe")).not.toBeNull();
  });

  it("renders PNG tiles in annotation mode", async () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<Canvas model={model} mode="annotation" resolver={resolver} />);
    await act(async () => { await sleep(50); });
    expect(container.querySelector("img")).not.toBeNull();
    expect(container.querySelector("iframe")).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/Canvas.browser.test.tsx`
Expected: FAIL — `Canvas` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/Canvas.tsx`**(API 名执行期核对)

```tsx
import { useMemo } from "react";
import {
  ReactFlow,
  ReactFlowProvider,
  Background,
  type Node,
  type NodeProps
} from "@xyflow/react"; // UNCONFIRMED: 执行期 Context7 核对 v12 导出名
import "@xyflow/react/dist/style.css"; // UNCONFIRMED: 核对 CSS 路径

import type { CanvasMode, PositionedTile, ResourceResolver, ViewerModel } from "./model.js";
import { DesignTile } from "./tiles/DesignTile.js";
import { AnnotationTile } from "./tiles/AnnotationTile.js";

export interface CanvasProps {
  model: ViewerModel;
  mode: CanvasMode;
  resolver: ResourceResolver;
}

/** 自定义节点 data 载荷。 */
interface TileNodeData extends Record<string, unknown> {
  tile: PositionedTile;
  mode: CanvasMode;
  resolver: ResourceResolver;
}

type TileNode = Node<TileNodeData, "tile">;

/** React Flow 自定义节点:按模式选渲染器。 */
function TileNodeComponent({ data }: NodeProps<TileNode>): React.ReactElement {
  return data.mode === "design" ? (
    <DesignTile tile={data.tile} resolver={data.resolver} />
  ) : (
    <AnnotationTile tile={data.tile} resolver={data.resolver} />
  );
}

const nodeTypes = { tile: TileNodeComponent };

export function Canvas({ model, mode, resolver }: CanvasProps): React.ReactElement {
  const nodes = useMemo<TileNode[]>(
    () =>
      model.tiles.map((tile) => ({
        id: tile.id,
        type: "tile",
        position: { x: tile.x, y: tile.y },
        data: { tile, mode, resolver },
        // 给 React Flow 显式尺寸,利于视口虚拟化量测
        width: tile.width,
        height: tile.height,
        draggable: false,
        selectable: true,
        connectable: false
      })),
    [model.tiles, mode, resolver]
  );

  // 只渲视口内 tile;不要默认 fitView,否则全图缩进视口会破坏离屏卸载验收。
  return (
    <ReactFlowProvider>
      <div style={{ width: "100%", height: "100%" }}>
        <ReactFlow
          nodes={nodes}
          edges={[]}
          nodeTypes={nodeTypes}
          onlyRenderVisibleElements
          nodesDraggable={false}
          nodesConnectable={false}
          elementsSelectable
          proOptions={{ hideAttribution: false }}
        >
          <Background />
        </ReactFlow>
      </div>
    </ReactFlowProvider>
  );
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

追加:
```ts
export { Canvas } from "./Canvas.js";
export type { CanvasProps } from "./Canvas.js";
```

- [ ] **Step 5: 跑测试确认通过**(若 React Flow 选择器名与核对结论不符,据实更新测试里的 `.react-flow__node` 选择器)

Run: `pnpm test --project viewer packages/viewer/src/Canvas.browser.test.tsx`
Expected: PASS(2 tests)。

- [ ] **Step 6: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/Canvas.tsx packages/viewer/src/index.ts packages/viewer/src/Canvas.browser.test.tsx
git commit -m "feat(p7): React Flow infinite-canvas shell with viewport virtualization

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: 左侧设计稿列表 `DesignList` + 定位跳转

**⚠️ 定位用 `useReactFlow().setCenter`,执行期 Context7 核对 v12 API。生产 Canvas 不默认 `fitView`,避免破坏区域虚拟化。**

**Files:**
- Create: `packages/viewer/src/DesignList.tsx`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/src/DesignList.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/DesignList.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it, vi } from "vitest";
import { DesignList, buildViewerModel } from "@xenonbyte/forma-viewer";
import type { NormalizeArtifactInput } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});

const artifacts: NormalizeArtifactInput[] = [
  { artifactId: "a", kind: "design-page", pageId: "login", pageName: "登录页", variant: "default", title: "登录页 默认", version: 1, width: 600, height: 400 },
  { artifactId: "a", kind: "design-page", pageId: "login", pageName: "登录页", variant: "wide", title: "登录页 宽屏", version: 1, width: 600, height: 400 },
  { artifactId: "b", kind: "design-page", pageId: "home", pageName: "首页", variant: "default", title: "首页", version: 1, width: 600, height: 400 }
];

describe("DesignList", () => {
  it("lists each page group with its variants", () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<DesignList model={model} onLocate={() => {}} />);
    expect(container.textContent).toContain("登录页");
    expect(container.textContent).toContain("首页");
    // variant 行可点击,数量 = tile 数
    const items = container.querySelectorAll("[data-tile-id]");
    expect(items.length).toBe(3);
  });

  it("calls onLocate with the tile id when a variant row is clicked", () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const onLocate = vi.fn();
    const container = render(<DesignList model={model} onLocate={onLocate} />);
    const row = container.querySelector<HTMLElement>('[data-tile-id="a:1:wide"]')!;
    act(() => row.click());
    expect(onLocate).toHaveBeenCalledWith("a:1:wide");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/DesignList.browser.test.tsx`
Expected: FAIL — `DesignList` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/DesignList.tsx`**

```tsx
import type { ViewerModel } from "./model.js";

export interface DesignListProps {
  model: ViewerModel;
  /** 点击某 variant 行 → 通知外壳定位/跳转到对应 tile。 */
  onLocate: (tileId: string) => void;
}

/** 左侧设计稿列表:按 page 分组,组内列 variant;点击行触发定位。 */
export function DesignList({ model, onLocate }: DesignListProps): React.ReactElement {
  const titleById = new Map(model.tiles.map((t) => [t.id, t]));
  return (
    <nav aria-label="设计稿列表" style={{ overflowY: "auto", height: "100%", padding: 8 }}>
      {model.groups.map((group) => (
        <section key={group.pageId} style={{ marginBottom: 12 }}>
          <h3 style={{ fontSize: 13, fontWeight: 600, margin: "4px 0" }}>{group.pageName}</h3>
          <ul style={{ listStyle: "none", margin: 0, padding: 0 }}>
            {group.tileIds.map((tileId) => {
              const tile = titleById.get(tileId);
              return (
                <li key={tileId}>
                  <button
                    type="button"
                    data-tile-id={tileId}
                    onClick={() => onLocate(tileId)}
                    style={{
                      display: "block",
                      width: "100%",
                      textAlign: "left",
                      padding: "4px 8px",
                      border: "none",
                      background: "transparent",
                      cursor: "pointer",
                      fontSize: 12
                    }}
                  >
                    {tile?.variant ?? tileId}
                  </button>
                </li>
              );
            })}
          </ul>
        </section>
      ))}
    </nav>
  );
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

追加:
```ts
export { DesignList } from "./DesignList.js";
export type { DesignListProps } from "./DesignList.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test --project viewer packages/viewer/src/DesignList.browser.test.tsx`
Expected: PASS(2 tests)。

- [ ] **Step 6: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/DesignList.tsx packages/viewer/src/index.ts packages/viewer/src/DesignList.browser.test.tsx
git commit -m "feat(p7): left design list with locate callback

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: 右侧标注信息栏 `AnnotationSlot`(预留空占位)

**Files:**
- Create: `packages/viewer/src/AnnotationSlot.tsx`
- Modify: `packages/viewer/src/index.ts`
- Test: `packages/viewer/src/AnnotationSlot.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/AnnotationSlot.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { AnnotationSlot } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});

describe("AnnotationSlot", () => {
  it("renders a reserved region with the annotation role", () => {
    const container = render(<AnnotationSlot />);
    const region = container.querySelector('[data-slot="annotation"]');
    expect(region).not.toBeNull();
  });

  it("is empty of annotation content this phase (placeholder only)", () => {
    const container = render(<AnnotationSlot />);
    const region = container.querySelector('[data-slot="annotation"]')!;
    // 仅占位文案,无标注条目
    expect(region.querySelectorAll('[data-annotation-item]').length).toBe(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/AnnotationSlot.browser.test.tsx`
Expected: FAIL — `AnnotationSlot` 未导出。

- [ ] **Step 3: 写 `packages/viewer/src/AnnotationSlot.tsx`**

```tsx
/**
 * 右侧标注信息栏:本期仅预留 slot,不实现标注内容(由后续阶段填充)。
 */
export function AnnotationSlot(): React.ReactElement {
  return (
    <aside
      data-slot="annotation"
      aria-label="标注信息栏"
      style={{ height: "100%", padding: 12, color: "#888", fontSize: 12 }}
    >
      标注功能待后续版本提供。
    </aside>
  );
}
```

- [ ] **Step 4: 从 `src/index.ts` 导出**

追加:
```ts
export { AnnotationSlot } from "./AnnotationSlot.js";
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test --project viewer packages/viewer/src/AnnotationSlot.browser.test.tsx`
Expected: PASS(2 tests)。

- [ ] **Step 6: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/AnnotationSlot.tsx packages/viewer/src/index.ts packages/viewer/src/AnnotationSlot.browser.test.tsx
git commit -m "feat(p7): reserved right annotation slot (empty placeholder)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: 顶层 `Viewer` 组件(三栏装配 + 画布模式切换)

**⚠️ 定位跳转用 React Flow imperative API,执行期 Context7 核对。**

**Files:**
- Create: `packages/viewer/src/Viewer.tsx`
- Modify: `packages/viewer/src/index.ts`
- Modify: `packages/viewer/src/Canvas.tsx`(暴露 `locate` 能力 via ref/回调,见 Step 3)
- Test: `packages/viewer/src/Viewer.browser.test.tsx`

- [ ] **Step 1: 写失败测试 `packages/viewer/src/Viewer.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { Viewer, buildViewerModel } from "@xenonbyte/forma-viewer";
import type { NormalizeArtifactInput, ResourceResolver } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  container.style.width = "1200px";
  container.style.height = "800px";
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});
const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));

const artifacts: NormalizeArtifactInput[] = [
  { artifactId: "a", kind: "design-page", pageId: "login", pageName: "登录页", variant: "default", title: "登录页", version: 1, width: 600, height: 400 },
  { artifactId: "b", kind: "design-page", pageId: "home", pageName: "首页", variant: "default", title: "首页", version: 1, width: 600, height: 400 }
];
const resolver: ResourceResolver = { resolve: (ref) => `https://example.test/${ref.artifactId}/${ref.kind}` };

describe("Viewer", () => {
  it("renders left list, center canvas, and right annotation slot", async () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<Viewer model={model} resolver={resolver} />);
    await act(async () => { await sleep(50); });
    expect(container.querySelector('nav[aria-label="设计稿列表"]')).not.toBeNull();
    expect(container.querySelector(".react-flow")).not.toBeNull();
    expect(container.querySelector('[data-slot="annotation"]')).not.toBeNull();
  });

  it("defaults to design mode (iframe) and switches to annotation mode (img)", async () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<Viewer model={model} resolver={resolver} />);
    await act(async () => { await sleep(50); });
    expect(container.querySelector("iframe")).not.toBeNull();

    const toggle = container.querySelector<HTMLElement>('[data-action="mode-annotation"]')!;
    await act(async () => { toggle.click(); await sleep(50); });
    expect(container.querySelector("img")).not.toBeNull();
    expect(container.querySelector("iframe")).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test --project viewer packages/viewer/src/Viewer.browser.test.tsx`
Expected: FAIL — `Viewer` 未导出。

- [ ] **Step 3: 让 `Canvas` 接受 `locateTileId` prop 以支持外部定位**

在 `packages/viewer/src/Canvas.tsx`:
- `CanvasProps` 增加可选 `locateTileId?: string | null`。
- 用内部组件包一层以便调用 `useReactFlow()`(provider 内部才能用 hook):

把 `Canvas` 重构为:
```tsx
import { useEffect, useMemo } from "react";
import {
  ReactFlow,
  ReactFlowProvider,
  Background,
  useReactFlow,
  type Node,
  type NodeProps
} from "@xyflow/react"; // UNCONFIRMED 核对
import "@xyflow/react/dist/style.css";

import type { CanvasMode, PositionedTile, ResourceResolver, ViewerModel } from "./model.js";
import { DesignTile } from "./tiles/DesignTile.js";
import { AnnotationTile } from "./tiles/AnnotationTile.js";

export interface CanvasProps {
  model: ViewerModel;
  mode: CanvasMode;
  resolver: ResourceResolver;
  /** 外部要求定位到的 tile id;变化时居中到该 tile。 */
  locateTileId?: string | null;
}

interface TileNodeData extends Record<string, unknown> {
  tile: PositionedTile;
  mode: CanvasMode;
  resolver: ResourceResolver;
}
type TileNode = Node<TileNodeData, "tile">;

function TileNodeComponent({ data }: NodeProps<TileNode>): React.ReactElement {
  return data.mode === "design" ? (
    <DesignTile tile={data.tile} resolver={data.resolver} />
  ) : (
    <AnnotationTile tile={data.tile} resolver={data.resolver} />
  );
}
const nodeTypes = { tile: TileNodeComponent };

function CanvasInner({ model, mode, resolver, locateTileId }: CanvasProps): React.ReactElement {
  const rf = useReactFlow();
  const nodes = useMemo<TileNode[]>(
    () =>
      model.tiles.map((tile) => ({
        id: tile.id,
        type: "tile",
        position: { x: tile.x, y: tile.y },
        data: { tile, mode, resolver },
        width: tile.width,
        height: tile.height,
        draggable: false,
        selectable: true,
        connectable: false
      })),
    [model.tiles, mode, resolver]
  );

  useEffect(() => {
    if (!locateTileId) return;
    const tile = model.tiles.find((t) => t.id === locateTileId);
    if (!tile) return;
    // 居中到 tile 中心。setCenter 签名执行期核对。
    rf.setCenter(tile.x + tile.width / 2, tile.y + tile.height / 2, { zoom: 1, duration: 300 });
  }, [locateTileId, model.tiles, rf]);

  // 只渲视口内 tile;不要默认 fitView,否则全图缩进视口会破坏离屏卸载验收。
  return (
    <ReactFlow
      nodes={nodes}
      edges={[]}
      nodeTypes={nodeTypes}
      onlyRenderVisibleElements
      nodesDraggable={false}
      nodesConnectable={false}
      elementsSelectable
    >
      <Background />
    </ReactFlow>
  );
}

export function Canvas(props: CanvasProps): React.ReactElement {
  return (
    <ReactFlowProvider>
      <div style={{ width: "100%", height: "100%" }}>
        <CanvasInner {...props} />
      </div>
    </ReactFlowProvider>
  );
}
```

> 重构后重跑 Task 8 测试确保不回归:`pnpm test --project viewer packages/viewer/src/Canvas.browser.test.tsx` → PASS。

- [ ] **Step 4: 写 `packages/viewer/src/Viewer.tsx`**

```tsx
import { useState } from "react";
import { Canvas } from "./Canvas.js";
import { DesignList } from "./DesignList.js";
import { AnnotationSlot } from "./AnnotationSlot.js";
import type { CanvasMode, ResourceResolver, ViewerModel } from "./model.js";

export interface ViewerProps {
  model: ViewerModel;
  resolver: ResourceResolver;
}

/** 顶层只读查看器:左 设计稿列表 / 中 画布 / 右 标注 slot + 画布模式切换。 */
export function Viewer({ model, resolver }: ViewerProps): React.ReactElement {
  const [mode, setMode] = useState<CanvasMode>("design");
  const [locateTileId, setLocateTileId] = useState<string | null>(null);

  return (
    <div style={{ display: "grid", gridTemplateColumns: "220px 1fr 280px", height: "100%", width: "100%" }}>
      <div style={{ borderRight: "1px solid #eee", minWidth: 0 }}>
        <DesignList model={model} onLocate={setLocateTileId} />
      </div>
      <div style={{ position: "relative", minWidth: 0 }}>
        <div style={{ position: "absolute", top: 8, right: 8, zIndex: 10, display: "flex", gap: 4 }}>
          <button
            type="button"
            data-action="mode-design"
            onClick={() => setMode("design")}
            aria-pressed={mode === "design"}
          >
            设计
          </button>
          <button
            type="button"
            data-action="mode-annotation"
            onClick={() => setMode("annotation")}
            aria-pressed={mode === "annotation"}
          >
            标注
          </button>
        </div>
        <Canvas model={model} mode={mode} resolver={resolver} locateTileId={locateTileId} />
      </div>
      <div style={{ borderLeft: "1px solid #eee", minWidth: 0 }}>
        <AnnotationSlot />
      </div>
    </div>
  );
}
```

- [ ] **Step 5: 从 `src/index.ts` 导出**

追加:
```ts
export { Viewer } from "./Viewer.js";
export type { ViewerProps } from "./Viewer.js";
```

- [ ] **Step 6: 跑测试确认通过**

Run: `pnpm test --project viewer packages/viewer/src/Viewer.browser.test.tsx`
Expected: PASS(2 tests)。

- [ ] **Step 7: 类型检查 + Commit**

Run: `pnpm --filter @xenonbyte/forma-viewer typecheck` → PASS
```bash
git add packages/viewer/src/Viewer.tsx packages/viewer/src/Canvas.tsx packages/viewer/src/index.ts packages/viewer/src/Viewer.browser.test.tsx
git commit -m "feat(p7): top-level Viewer (3-pane shell + canvas mode toggle + locate)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 12: 区域虚拟化验证(离屏卸载)+ README 契约文档定稿

**Files:**
- Test: `packages/viewer/src/Canvas.virtualization.browser.test.tsx`
- Modify: `packages/viewer/README.md`(补全 view-model 契约)

- [ ] **Step 1: 写区域虚拟化测试 `packages/viewer/src/Canvas.virtualization.browser.test.tsx`**

```tsx
import { act } from "react";
import { createRoot, type Root } from "react-dom/client";
import { afterEach, describe, expect, it } from "vitest";
import { Canvas, buildViewerModel } from "@xenonbyte/forma-viewer";
import type { NormalizeArtifactInput, ResourceResolver } from "@xenonbyte/forma-viewer";

(globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
const roots: Root[] = [];
const containers: HTMLElement[] = [];
function render(ui: React.ReactElement): HTMLElement {
  const container = document.createElement("div");
  container.style.width = "800px";
  container.style.height = "600px";
  document.body.appendChild(container);
  const root = createRoot(container);
  act(() => root.render(ui));
  roots.push(root);
  containers.push(container);
  return container;
}
afterEach(() => {
  for (const root of roots.splice(0)) act(() => root.unmount());
  for (const container of containers.splice(0)) container.remove();
});
const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));

// 造 40 个分散在大画布上的 tile,远超视口
const artifacts: NormalizeArtifactInput[] = Array.from({ length: 40 }, (_, i) => ({
  artifactId: `art${i}`,
  kind: "design-page" as const,
  pageId: `page${i}`,
  pageName: `页 ${i}`,
  variant: "default",
  title: `页 ${i}`,
  version: 1,
  width: 1200,
  height: 900
}));
const resolver: ResourceResolver = { resolve: (ref) => `https://example.test/${ref.artifactId}/${ref.kind}` };

describe("Canvas viewport virtualization", () => {
  it("mounts far fewer node iframes than total tiles (offscreen unmounted)", async () => {
    const model = buildViewerModel({ entry: "requirement", artifacts });
    const container = render(<Canvas model={model} mode="design" resolver={resolver} />);
    // Canvas 生产代码不得默认 fitView;默认/受控初始视口只覆盖局部区域。
    await act(async () => { await sleep(80); });
    const renderedNodes = container.querySelectorAll(".react-flow__node");
    // onlyRenderVisibleElements 应让挂载节点数远小于 40
    expect(renderedNodes.length).toBeGreaterThan(0);
    expect(renderedNodes.length).toBeLessThan(40);
  });
});
```

> 注:`fitView` 会把全部 tile 缩进视口,使"可见"节点可能等于总数,因此生产 `Canvas` 默认禁止 `fitView`。若执行期需显式初始视口,只能用经 Context7 核对的 bounded viewport/defaultViewport 策略,并保持本测试语义:**离屏 tile 不应全部挂载**。

- [ ] **Step 2: 跑测试确认行为**

Run: `pnpm test --project viewer packages/viewer/src/Canvas.virtualization.browser.test.tsx`
Expected: PASS——挂载节点数 > 0 且 < 40;若等于 40,停止并修正生产 Canvas 的初始视口策略,不得用默认 `fitView` 绕过。

- [ ] **Step 3: 补全 `README.md` 的契约章节**

把 README 的「## 契约」节替换为:
```markdown
## 契约(view-model)

viewer 是纯展示组件,消费方负责把自身数据源映射成 `ViewerModel` 并注入 `ResourceResolver`。

### 顶层用法
```tsx
import { Viewer, buildViewerModel } from "@xenonbyte/forma-viewer";
import type { ResourceResolver, NormalizeArtifactInput } from "@xenonbyte/forma-viewer";

const artifacts: NormalizeArtifactInput[] = /* 由 web(HTTP) 或 desktop(IPC) 数据映射 */;
const model = buildViewerModel({ entry: "requirement", artifacts });
const resolver: ResourceResolver = {
  resolve: (ref) => /* web: HTTP URL;desktop: app:// 或 IPC URL */
};

<Viewer model={model} resolver={resolver} />;
```

### 关键类型
- `NormalizeArtifactInput`:中性单 artifact(artifactId/kind/pageId/pageName/variant/title/version/width/height)。
- `ViewerModel`:`{ entry, tiles: PositionedTile[], groups: ViewerGroup[] }`。
- `ResourceRef`:不透明引用 `{ artifactId, version, kind: "bundle"|"preview"|"asset", density?, path? }`。
- `PreviewImageRefs`:`{ "1x": ResourceRef, "2x": ResourceRef }`;标注 PNG 必须保留 1x/2x 两档。
- `ResourceResolver.resolve(ref) => string`:消费方实现(web HTTP / desktop IPC)。

### 消费方如何从 Forma 后端映射(P8/P9 接入指引)
后端 `get_product_artifact(product_id, artifact_id)` 已返回/必须在 P4 暴露(执行期按实际工具字段核对):
`{ manifest, bundle_url, assets: [{ path, role, density, degraded?, urls: { "1x"|"2x"|"3x": url } }], preview_urls: { "1x": url, "2x": url }, versions: number[], current_version }`;
`list_product_artifacts` 每条含 `{ id, kind, title, requirement_id, page_id, variant, versions, current_version, preview_urls, superseded }`。
- `NormalizeArtifactInput.{artifactId,kind,pageId,variant,version}` ← 上述 `id/kind/page_id/variant/current_version`。
- `ResourceResolver.resolve`:`kind:"bundle"` → 后端 `bundle_url`;`kind:"preview", density:"1x"|"2x"` → `preview_urls[density]`;`kind:"asset"` → `assets[].urls[density]`(按 `path` 匹配)。web 直接用这些相对 URL;desktop 经只读 preload bridge 暴露为可取 URL。
- `width/height` 不在后端返回中 → 消费方按平台/约定提供(或后续在 manifest 增字段);P8/P9 决定来源,本期 viewer 只消费。

### 不变量
- 设计画布 tile = `<iframe sandbox>`(无 `allow-scripts`);标注画布 tile = `<img>` PNG,`src` 指向 1x、`srcSet` 指向 2x。
- 画布区域虚拟化:`onlyRenderVisibleElements`,离屏 tile 卸载。
- 右侧标注 slot 本期仅占位。
- 契约变更必须同步 web/desktop 两端 + 测试(唯一耦合点)。
```

- [ ] **Step 4: 跑 viewer 全部测试**

Run: `pnpm test --project viewer`
Expected: 全绿(smoke + DesignTile 3 + AnnotationTile 4 + Canvas 2 + DesignList 2 + AnnotationSlot 2 + Viewer 2 + virtualization 1)。

- [ ] **Step 5: Commit**

```bash
git add packages/viewer/src/Canvas.virtualization.browser.test.tsx packages/viewer/README.md
git commit -m "test(p7): viewport virtualization coverage + finalize contract docs

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 13: 全量验收(build / typecheck / test 全绿)

**Files:**（不改源码,仅验收;如发现问题回到相应 task 修)

- [ ] **Step 1: 全量构建**

Run: `pnpm build`
Expected: exit 0;viewer 无 `build` script 被 `--if-present` 跳过,其余包构建成功。

- [ ] **Step 2: 全量类型检查**

Run: `pnpm typecheck`
Expected: 所有包 `typecheck: Done`,含 `packages/viewer`;exit 0。

- [ ] **Step 3: 全量测试**

Run: `pnpm test`
Expected: 全绿。基线 934 + viewer node 测试(model 2 + normalize 4 + layout 3 = 9)+ viewer 浏览器测试(smoke 1 + DesignTile 3 + AnnotationTile 4 + Canvas 2 + DesignList 2 + AnnotationSlot 2 + Viewer 2 + virtualization 1 = 17)。**确认 0 failed,且 unit/viewer 两个 project 都跑到。**

- [ ] **Step 4: 确认无意外改动 / 工作树状态**

Run: `git status --short`
Expected: 干净(全部已提交)。

- [ ] **Step 5: 最终 reviewer 子代理审查整支分支**(subagent-driven-development 收尾要求)

按 superpowers:requesting-code-review 对 `feat/p7-viewer` 全量 diff 做一次最终代码审查;有问题回相应 task 修并补测试。

- [ ] **Step 6: 收尾**

按 superpowers:finishing-a-development-branch 处理分支收尾(由用户决定何时 merge 到 main + 删分支——沿用既往 per-phase 流程,**不自动 merge**)。

---

## 自审(against spec 第 119–128、157–159、175 行)

**1. Spec 覆盖:**
- 无限画布外壳(React Flow) → Task 8/11 ✅
- 左=设计稿列表(page→variant,点击定位) → Task 9 ✅
- 中=画布(pan/zoom 本地态,范围随入口) → Task 8(React Flow 自带 pan/zoom)+ normalize 的 entry 透传 ✅
- 右=标注 slot(预留不实现) → Task 10 ✅
- 区域渲染 `onlyRenderVisibleElements`(离屏卸载) → Task 8 + Task 12 验证 ✅
- 设计画布静态 HTML(sandbox 禁脚本) → Task 6 ✅
- 标注画布 PNG → Task 7 ✅
- 规范化数据契约(web/desktop 唯一耦合点) → Task 2/3 + README(Task 12) ✅
- 资源解析器注入 → `ResourceResolver`(Task 2)贯穿 Task 6/7/8/11 ✅
- 组件可测(React Flow 需真实 DOM → vitest 浏览器模式) → Task 5 基建 + 各组件浏览器测试 ✅
- 左列表点选定位到 tile → Task 9(回调)+ Task 11(`setCenter`)✅
- PNG 与 HTML 同按 version 绑定 + 标注 PNG 1x/2x LOD → 同一 tile 持 `htmlBundle`+`previewImages["1x"/"2x"]` 同 version(Task 2/3/7)✅

**2. Placeholder 扫描:** 无 TBD/TODO;每个代码 step 给出完整代码;React Flow / Vitest browser 的版本敏感 API 已显式标 `UNCONFIRMED` 并指派执行期 Context7 核对 step——这是有意的、非占位。

**3. 类型一致性:** `ViewerTile`/`PositionedTile`/`ViewerModel`/`ResourceRef`/`ResourceResolver`/`NormalizeArtifactInput`/`CanvasMode` 在 Task 2 定义,Task 3/4/6/7/8/9/11 一致复用;`buildViewerModel` 入参 `{ entry, artifacts }`、`layoutTiles(tiles, groups)`、`resolver.resolve(ref)`、`onLocate(tileId)`、`Canvas` 的 `locateTileId` 全程一致。

**已知风险(执行期处理):**
- Context7 不可用 → React Flow / Vitest browser 的精确 API 待执行期核对;若届时仍不可用,用官方文档/源码核对并记 `CONTINUITY.md`。
- 生产 Canvas 默认 `fitView` 会令虚拟化测试全可见 → Task 8/11 已禁止默认 `fitView`,Task 12 必须以局部视口断言离屏卸载。
- Vitest 4 `projects` + Playwright provider 为新基建,Task 5 设为显式 gate(现有 934 测试必须不回归)。
